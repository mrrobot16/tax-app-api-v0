{
    "name": "irs-copilot-api",
    "license": "MIT",
    "scripts": {
        "start": "python main.py",
        "dev": "uvicorn app.server:app --reload",
        "prod": "uvicorn main:app",
        "push": "git push origin dev",
        "deploy": "git push heroku dev:master",
        "logs":"heroku logs --tail",
        "task_app": "cd task_app && yarn dev",
        "test_app": "cd test_app && yarn dev",
        "tests": "cd test_app && yarn test"
    }
}